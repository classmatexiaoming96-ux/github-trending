# GitHub Trending 项目深度解析 — 任务模板

> 每新增一个项目，克隆其源码后，填写本模板生成 `_cc_task.md`。
> 效铭要求：分析要有"每层实现细节"，CC 不能只泛泛提及架构。

---

## 必须分析的文件（按优先级）

按以下顺序读取源码，缺一不可：

1. **入口文件**（`app.py` / `main.go` / `index.ts` 等）— 理解整体架构
2. **核心业务模块**（至少 3 个）— 理解数据流和关键逻辑
3. **边界/错误处理**（至少 2 处）— 理解失败模式
4. **基础设施层**（如果存在：DB、队列、网络）— 理解运维能力

禁止只读 README 就下结论。

---

## 输出要求：每个页面必须包含

| 章节 | 最低要求 | 验收标准 |
|------|---------|---------|
| **一句话定位** | 用一行说清楚这个项目解决什么问题 | 效铭能复述 |
| **架构流程图** | 分层 ASCII 图，标注数据流向 | 能看出数据从哪进、从哪出 |
| **核心模块拆解** | 至少 4 个模块，每个含：文件名 + 5-15行关键代码 + 为什么这样设计 | 有代码有解释 |
| **关键技术细节** | 至少 3 个，每个讲清工程取舍（为什么用这个方案而非另一个） | 读者能复现 |
| **失败模式** | 至少 2 个具体场景，说明系统如何处理 | 有具体错误信息/日志 |
| **竞品对比** | 与同类项目的差异化（至少 2 点） | 说清取舍 |
| **生态意义** | 对 coding agent 生态的影响 | 能看出定位 |

---

## HTML 页面规范

```
output file: {project-name}.html（standalone 页面，可独立访问）
tab panel: data-tab="{tab_id}"，嵌入 index.html 中
style: 与现有页面一致（深色主题、管道图、代码高亮）
```

每个页面必须包含：
- `<h1>` 项目名 + 一句话定位
- `.statstrip` 或 `.statrow` 统计数据栏（Stars、技术栈、核心指标）
- `.pipe` 或 ASCII 架构流程图
- 至少 4 个 `.code` 代码块（带文件名 bar）
- 至少 2 个 `.callout` 或 `.card` 高亮框

---

## 禁止事项

- ❌ 只读 README 就写结论
- ❌ 泛泛提及"该模块很重要"，不写具体实现
- ❌ 用"使用了 XXX 技术"代替数据流说明
- ❌ 缺少 failure mode 分析

---

## 示例：高质量输出的标志

如果项目有 4 层流水线，必须：
1. 画出各层边界和依赖关系
2. 说明每层的数据格式（进了什么、出了什么）
3. 指出哪层最可能成为瓶颈
4. 给出源码中的具体文件名和行号

如果项目有异步队列，必须：
1. 说明队列的触发条件
2. 描述消费失败时的重试策略
3. 指出积压时的背压机制

---

## 执行流程

1. 克隆源码到 `sources/{project}/`
2. 填写下方项目信息
3. 生成 `_cc_task.md`
4. CC 执行完成后验证页面行数 ≥ 300 行
5. 验证每个章节都有实质性内容（不是一句话带过）
6. git push

---

## 项目信息（每新增一个项目则填写）

```
## 任务：为 {project} 生成深度解析页面

### 项目信息
- GitHub：{url}
- Stars：{stars}★
- 技术栈：{tech_stack}
- 已克隆路径：`/root/repos/github-trending/sources/{project}/`
- 输出文件：`{project}.html`
- Tab ID：`{tab_id}`

### 必须分析的文件（按优先级）

优先级 1 — 入口和核心：
- {file_path_1}：说明 {why}
- {file_path_2}：说明 {why}
- {file_path_3}：说明 {why}

优先级 2 — 关键业务逻辑：
- {file_path_4}：说明 {why}
- {file_path_5}：说明 {why}

优先级 3 — 基础设施和边界：
- {file_path_6}：说明 {why}
- {file_path_7}：说明 {why}

### 架构流程图

描述该项目的多层架构，用 ASCII 画出示意图：

[层1] → [层2] → [层3]

标注每层的关键组件和数据格式。

### 核心模块拆解要求

必须包含以下 4 个模块的深度分析：

**模块 A**：`{file_path}` — {模块功能}
- 关键代码段（5-15行）
- 数据流说明
- 工程取舍（为什么这样设计）

**模块 B**：`{file_path}` — {模块功能}
...（同理至少 4 个）

### 关键技术细节（至少 3 个）

1. **{技术名}**：{做什么} — {为什么用这个方案} — {替代方案及取舍}
2. **{技术名}**：{做什么} — {为什么用这个方案} — {替代方案及取舍}
3. **{技术名}**：{做什么} — {为什么用这个方案} — {替代方案及取舍}

### 失败模式（至少 2 个）

**场景 1**：`{什么情况触发}` → `{系统如何处理}` → `{结果}`

**场景 2**：`{什么情况触发}` → `{系统如何处理}` → `{结果}`

### 输出规范

- 文件：`/root/repos/github-trending/{project}.html`
- 风格：与 kimi-code.html（429行）同等深度
- 验证：页面行数 ≥ 300 行，每个章节都有实质性内容
- push：`git add -A && git commit -m "feat: deep-dive {project}" && git push`
```