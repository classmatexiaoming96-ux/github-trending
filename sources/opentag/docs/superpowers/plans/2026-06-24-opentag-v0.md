# OpenTag V0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the first working OpenTag vertical slice: a GitHub-style mention becomes an auditable run, a local daemon claims it, an echo executor runs it, and a callback summary is produced.

**Architecture:** Use a TypeScript pnpm monorepo with dependency-free core schemas, a thin dispatcher backed by SQLite/Drizzle, a Probot GitHub ingress app, and a local `opentagd` daemon that polls and executes runs. Keep framework dependencies at the edges so `opentag-core` remains portable.

**Tech Stack:** TypeScript, pnpm workspaces, tsup, Vitest, Zod, Hono, Drizzle ORM, SQLite/libSQL, Probot, Octokit, Commander.

---

## Defaults Locked For V0

- Implementation language: TypeScript only.
- Dispatcher: hosted HTTP service with SQLite-compatible persistence.
- Runner communication: polling plus leases, not inbound local webhooks.
- First executor: `echo`, then Codex/Claude Code/oh-my-pi adapters after the loop works.
- GitHub ingress: Probot at the edge.
- Core schema: Zod-first, JSON Schema export later.
- Storage: Drizzle schema over SQLite/libSQL.
- Write operations: v0 posts comments only; PR creation is Milestone 4.

## File Structure

- Create `package.json`: root scripts and workspace metadata.
- Create `pnpm-workspace.yaml`: package/app workspace globs.
- Create `tsconfig.base.json`: shared TS compiler settings.
- Create `vitest.config.ts`: shared test configuration.
- Create `packages/core/src/schema.ts`: Zod schemas for events, runs, permissions, callbacks, results.
- Create `packages/core/src/mention.ts`: parse `@opentag` commands into normalized command intent.
- Create `packages/core/src/index.ts`: public exports.
- Create `packages/core/test/mention.test.ts`: command parsing regression tests.
- Create `packages/core/test/schema.test.ts`: schema validation regression tests.
- Create `packages/store/src/schema.ts`: Drizzle tables for runs, run events, runners, repo bindings.
- Create `packages/store/src/repository.ts`: create run, append audit event, claim lease, heartbeat, complete run.
- Create `packages/store/src/index.ts`: public exports.
- Create `packages/store/test/repository.test.ts`: SQLite-backed repository tests.
- Create `packages/github/src/normalize.ts`: GitHub payload to `OpenTagEvent`.
- Create `packages/github/src/render.ts`: acknowledgement and final comment rendering.
- Create `packages/github/src/index.ts`: public exports.
- Create `packages/github/test/normalize.test.ts`: payload normalization tests.
- Create `packages/runner/src/executor.ts`: executor adapter interface.
- Create `packages/runner/src/echo.ts`: echo executor adapter.
- Create `packages/runner/src/index.ts`: public exports.
- Create `packages/runner/test/echo.test.ts`: echo executor tests.
- Create `apps/dispatcher/src/server.ts`: Hono HTTP server for run creation, runner polling, status updates.
- Create `apps/dispatcher/src/index.ts`: server entry point.
- Create `apps/dispatcher/test/server.test.ts`: API tests.
- Create `apps/opentagd/src/config.ts`: local daemon config loader.
- Create `apps/opentagd/src/client.ts`: dispatcher API client.
- Create `apps/opentagd/src/daemon.ts`: poll/claim/execute/report loop.
- Create `apps/opentagd/src/index.ts`: CLI entry point.
- Create `apps/opentagd/test/daemon.test.ts`: daemon loop tests with fake dispatcher.
- Create `apps/github-probot/src/app.ts`: Probot app handler.
- Create `apps/github-probot/src/index.ts`: Probot entry point.
- Create `apps/github-probot/test/app.test.ts`: mention handling tests.
- Create `examples/github-to-echo/README.md`: manual demo instructions.

## Task 1: Monorepo Skeleton

**Files:**
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `tsconfig.base.json`
- Create: `vitest.config.ts`
- Create: `.gitignore`

- [ ] **Step 1: Write root package metadata**

Create `package.json`:

```json
{
  "name": "opentag",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
```

- [ ] **Step 2: Write workspace config**

Create `pnpm-workspace.yaml`:

```yaml
packages:
  - "packages/*"
  - "apps/*"
```

- [ ] **Step 3: Write shared TypeScript config**

Create `tsconfig.base.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true
  }
}
```

- [ ] **Step 4: Write shared Vitest config**

Create `vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: ["packages/**/*.test.ts", "apps/**/*.test.ts"],
    globals: false
  }
});
```

- [ ] **Step 5: Write gitignore**

Create `.gitignore`:

```gitignore
node_modules/
dist/
.env
.env.*
!.env.example
*.db
*.db-shm
*.db-wal
.DS_Store
coverage/
```

- [ ] **Step 6: Install dependencies**

Run:

```bash
pnpm install
```

Expected: lockfile is created and install exits successfully.

- [ ] **Step 7: Verify empty test command**

Run:

```bash
pnpm test
```

Expected: Vitest exits without running tests or reports no tests found. If it exits non-zero because no tests exist, continue; Task 2 adds tests.

## Task 2: Core Schemas And Mention Parser

**Files:**
- Create: `packages/core/package.json`
- Create: `packages/core/tsconfig.json`
- Create: `packages/core/tsup.config.ts`
- Create: `packages/core/src/schema.ts`
- Create: `packages/core/src/mention.ts`
- Create: `packages/core/src/index.ts`
- Create: `packages/core/test/mention.test.ts`
- Create: `packages/core/test/schema.test.ts`

- [ ] **Step 1: Write failing mention parser tests**

Create `packages/core/test/mention.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { parseOpenTagMention } from "../src/mention";

describe("parseOpenTagMention", () => {
  it("parses fix intent after @opentag", () => {
    expect(parseOpenTagMention("@opentag fix this flaky test")).toEqual({
      matched: true,
      rawText: "fix this flaky test",
      intent: "fix",
      args: {}
    });
  });

  it("ignores comments without an opentag mention", () => {
    expect(parseOpenTagMention("please fix this")).toEqual({ matched: false });
  });

  it("supports multiline comments", () => {
    expect(parseOpenTagMention("context\n@opentag review this PR\nthanks")).toEqual({
      matched: true,
      rawText: "review this PR",
      intent: "review",
      args: {}
    });
  });
});
```

- [ ] **Step 2: Write failing schema tests**

Create `packages/core/test/schema.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { OpenTagEventSchema } from "../src/schema";

describe("OpenTagEventSchema", () => {
  it("accepts a valid GitHub event", () => {
    const parsed = OpenTagEventSchema.parse({
      id: "evt_1",
      source: "github",
      sourceEventId: "12345",
      receivedAt: "2026-06-24T00:00:00.000Z",
      actor: {
        provider: "github",
        providerUserId: "42",
        handle: "octocat"
      },
      target: {
        mention: "@opentag",
        agentId: "opentag"
      },
      command: {
        rawText: "fix this",
        intent: "fix",
        args: {}
      },
      context: [
        {
          kind: "github.issue",
          uri: "https://github.com/acme/demo/issues/1",
          visibility: "public"
        }
      ],
      permissions: [
        {
          scope: "issue:comment",
          reason: "reply to source thread"
        }
      ],
      callback: {
        provider: "github",
        uri: "https://api.github.com/repos/acme/demo/issues/1/comments"
      },
      metadata: {}
    });

    expect(parsed.source).toBe("github");
  });
});
```

- [ ] **Step 3: Run tests to verify they fail**

Run:

```bash
pnpm test packages/core/test/mention.test.ts packages/core/test/schema.test.ts
```

Expected: FAIL because `../src/mention` and `../src/schema` do not exist.

- [ ] **Step 4: Create core package metadata**

Create `packages/core/package.json`:

```json
{
  "name": "@opentag/core",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 5: Create package TypeScript and build config**

Create `packages/core/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

Create `packages/core/tsup.config.ts`:

```ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
  sourcemap: true,
  clean: true
});
```

- [ ] **Step 6: Implement schemas**

Create `packages/core/src/schema.ts`:

```ts
import { z } from "zod";

export const SourceSchema = z.enum(["github", "slack", "lark", "cli", "webhook"]);
export const ProviderSchema = z.enum(["github", "slack", "lark"]);

export const ActorIdentitySchema = z.object({
  provider: ProviderSchema,
  providerUserId: z.string().min(1),
  handle: z.string().min(1).optional(),
  displayName: z.string().min(1).optional(),
  organizationId: z.string().min(1).optional()
});

export const AgentTargetSchema = z.object({
  mention: z.string().min(1),
  agentId: z.string().min(1),
  executorHint: z.enum(["codex", "claude-code", "oh-my-pi", "custom"]).optional(),
  workspaceHint: z.string().min(1).optional()
});

export const OpenTagCommandSchema = z.object({
  rawText: z.string(),
  intent: z.enum(["fix", "review", "investigate", "explain", "run", "unknown"]),
  args: z.record(z.union([z.string(), z.boolean(), z.number()]))
});

export const ContextPointerSchema = z.object({
  kind: z.enum([
    "github.repo",
    "github.issue",
    "github.pull_request",
    "github.comment",
    "github.commit",
    "file",
    "url",
    "text"
  ]),
  uri: z.string().min(1),
  title: z.string().min(1).optional(),
  visibility: z.enum(["public", "private", "organization"])
});

export const PermissionGrantSchema = z.object({
  scope: z.enum([
    "repo:read",
    "repo:write",
    "issue:comment",
    "pr:create",
    "pr:update",
    "runner:local",
    "network:restricted"
  ]),
  reason: z.string().min(1),
  expiresAt: z.string().datetime().optional()
});

export const CallbackRouteSchema = z.object({
  provider: z.enum(["github", "slack", "lark", "webhook"]),
  uri: z.string().min(1),
  threadKey: z.string().min(1).optional()
});

export const OpenTagEventSchema = z.object({
  id: z.string().min(1),
  source: SourceSchema,
  sourceEventId: z.string().min(1),
  receivedAt: z.string().datetime(),
  actor: ActorIdentitySchema,
  target: AgentTargetSchema,
  command: OpenTagCommandSchema,
  context: z.array(ContextPointerSchema),
  permissions: z.array(PermissionGrantSchema),
  callback: CallbackRouteSchema,
  metadata: z.record(z.unknown())
});

export const OpenTagRunResultSchema = z.object({
  conclusion: z.enum(["success", "failure", "cancelled", "needs_human"]),
  summary: z.string(),
  changedFiles: z.array(z.string()).optional(),
  createdPullRequestUrl: z.string().url().optional(),
  artifacts: z.array(z.object({ title: z.string(), uri: z.string() })).optional(),
  verification: z
    .array(
      z.object({
        command: z.string(),
        outcome: z.enum(["passed", "failed", "not_run"]),
        excerpt: z.string().optional()
      })
    )
    .optional(),
  nextAction: z.string().optional()
});

export const OpenTagRunSchema = z.object({
  id: z.string().min(1),
  eventId: z.string().min(1),
  status: z.enum(["queued", "assigned", "running", "needs_approval", "succeeded", "failed", "cancelled"]),
  assignedRunnerId: z.string().min(1).optional(),
  executor: z.string().min(1).optional(),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
  result: OpenTagRunResultSchema.optional()
});

export type ActorIdentity = z.infer<typeof ActorIdentitySchema>;
export type AgentTarget = z.infer<typeof AgentTargetSchema>;
export type OpenTagCommand = z.infer<typeof OpenTagCommandSchema>;
export type ContextPointer = z.infer<typeof ContextPointerSchema>;
export type PermissionGrant = z.infer<typeof PermissionGrantSchema>;
export type CallbackRoute = z.infer<typeof CallbackRouteSchema>;
export type OpenTagEvent = z.infer<typeof OpenTagEventSchema>;
export type OpenTagRun = z.infer<typeof OpenTagRunSchema>;
export type OpenTagRunResult = z.infer<typeof OpenTagRunResultSchema>;
```

- [ ] **Step 7: Implement mention parser**

Create `packages/core/src/mention.ts`:

```ts
import type { OpenTagCommand } from "./schema";

type MentionMatch = { matched: false } | ({ matched: true } & OpenTagCommand);

const INTENTS: OpenTagCommand["intent"][] = ["fix", "review", "investigate", "explain", "run"];

export function parseOpenTagMention(body: string, mention = "@opentag"): MentionMatch {
  const escaped = mention.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const pattern = new RegExp(`${escaped}\\s+([^\\n\\r]+)`, "i");
  const match = body.match(pattern);

  if (!match?.[1]) {
    return { matched: false };
  }

  const rawText = match[1].trim();
  const firstWord = rawText.split(/\s+/, 1)[0]?.toLowerCase();
  const intent = INTENTS.includes(firstWord as OpenTagCommand["intent"])
    ? (firstWord as OpenTagCommand["intent"])
    : "unknown";

  return {
    matched: true,
    rawText,
    intent,
    args: {}
  };
}
```

- [ ] **Step 8: Export core modules**

Create `packages/core/src/index.ts`:

```ts
export * from "./mention";
export * from "./schema";
```

- [ ] **Step 9: Install package deps and run tests**

Run:

```bash
pnpm install
pnpm test packages/core/test/mention.test.ts packages/core/test/schema.test.ts
```

Expected: PASS.

## Task 3: Store And Lease Repository

**Files:**
- Create: `packages/store/package.json`
- Create: `packages/store/tsconfig.json`
- Create: `packages/store/tsup.config.ts`
- Create: `packages/store/src/schema.ts`
- Create: `packages/store/src/repository.ts`
- Create: `packages/store/src/index.ts`
- Create: `packages/store/test/repository.test.ts`

- [ ] **Step 1: Write failing repository test**

Create `packages/store/test/repository.test.ts`:

```ts
import { drizzle } from "drizzle-orm/better-sqlite3";
import Database from "better-sqlite3";
import { describe, expect, it } from "vitest";
import { createOpenTagRepository } from "../src/repository";
import { migrateSchema } from "../src/schema";

describe("OpenTag repository", () => {
  it("creates and claims a run once", async () => {
    const sqlite = new Database(":memory:");
    const db = drizzle(sqlite);
    migrateSchema(sqlite);
    const repo = createOpenTagRepository(db);

    await repo.registerRunner({ runnerId: "runner_1", name: "Local Runner" });
    await repo.createRepoBinding({
      provider: "github",
      owner: "acme",
      repo: "demo",
      runnerId: "runner_1"
    });

    await repo.createRun({
      id: "run_1",
      event: {
        id: "evt_1",
        source: "github",
        sourceEventId: "comment_1",
        receivedAt: "2026-06-24T00:00:00.000Z",
        actor: { provider: "github", providerUserId: "42", handle: "octocat" },
        target: { mention: "@opentag", agentId: "opentag" },
        command: { rawText: "fix this", intent: "fix", args: {} },
        context: [{ kind: "github.issue", uri: "https://github.com/acme/demo/issues/1", visibility: "public" }],
        permissions: [{ scope: "issue:comment", reason: "reply to source thread" }],
        callback: { provider: "github", uri: "https://api.github.com/repos/acme/demo/issues/1/comments" },
        metadata: { owner: "acme", repo: "demo" }
      }
    });

    const claimed = await repo.claimNextRun({ runnerId: "runner_1", leaseSeconds: 60 });
    expect(claimed?.id).toBe("run_1");
    expect(claimed?.status).toBe("assigned");

    const secondClaim = await repo.claimNextRun({ runnerId: "runner_1", leaseSeconds: 60 });
    expect(secondClaim).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
pnpm test packages/store/test/repository.test.ts
```

Expected: FAIL because store modules do not exist.

- [ ] **Step 3: Create store package metadata**

Create `packages/store/package.json`:

```json
{
  "name": "@opentag/store",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@opentag/core": "workspace:*",
    "better-sqlite3": "^11.7.0",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 4: Create store TS configs**

Create `packages/store/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

Create `packages/store/tsup.config.ts`:

```ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
  sourcemap: true,
  clean: true
});
```

- [ ] **Step 5: Implement Drizzle schema and migration helper**

Create `packages/store/src/schema.ts`:

```ts
import type Database from "better-sqlite3";
import { index, integer, sqliteTable, text } from "drizzle-orm/sqlite-core";

export const runs = sqliteTable(
  "runs",
  {
    id: text("id").primaryKey(),
    eventId: text("event_id").notNull(),
    status: text("status").notNull(),
    eventJson: text("event_json").notNull(),
    resultJson: text("result_json"),
    assignedRunnerId: text("assigned_runner_id"),
    executor: text("executor"),
    leaseExpiresAt: text("lease_expires_at"),
    createdAt: text("created_at").notNull(),
    updatedAt: text("updated_at").notNull()
  },
  (table) => ({
    statusIdx: index("runs_status_idx").on(table.status),
    runnerIdx: index("runs_runner_idx").on(table.assignedRunnerId)
  })
);

export const runEvents = sqliteTable("run_events", {
  id: integer("id").primaryKey({ autoIncrement: true }),
  runId: text("run_id").notNull(),
  type: text("type").notNull(),
  payloadJson: text("payload_json").notNull(),
  createdAt: text("created_at").notNull()
});

export const runners = sqliteTable("runners", {
  runnerId: text("runner_id").primaryKey(),
  name: text("name").notNull(),
  createdAt: text("created_at").notNull(),
  heartbeatAt: text("heartbeat_at")
});

export const repoBindings = sqliteTable("repo_bindings", {
  id: integer("id").primaryKey({ autoIncrement: true }),
  provider: text("provider").notNull(),
  owner: text("owner").notNull(),
  repo: text("repo").notNull(),
  runnerId: text("runner_id").notNull(),
  createdAt: text("created_at").notNull()
});

export function migrateSchema(sqlite: Database.Database): void {
  sqlite.exec(`
    CREATE TABLE IF NOT EXISTS runs (
      id TEXT PRIMARY KEY,
      event_id TEXT NOT NULL,
      status TEXT NOT NULL,
      event_json TEXT NOT NULL,
      result_json TEXT,
      assigned_runner_id TEXT,
      executor TEXT,
      lease_expires_at TEXT,
      created_at TEXT NOT NULL,
      updated_at TEXT NOT NULL
    );
    CREATE INDEX IF NOT EXISTS runs_status_idx ON runs(status);
    CREATE INDEX IF NOT EXISTS runs_runner_idx ON runs(assigned_runner_id);
    CREATE TABLE IF NOT EXISTS run_events (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      run_id TEXT NOT NULL,
      type TEXT NOT NULL,
      payload_json TEXT NOT NULL,
      created_at TEXT NOT NULL
    );
    CREATE TABLE IF NOT EXISTS runners (
      runner_id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      created_at TEXT NOT NULL,
      heartbeat_at TEXT
    );
    CREATE TABLE IF NOT EXISTS repo_bindings (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      provider TEXT NOT NULL,
      owner TEXT NOT NULL,
      repo TEXT NOT NULL,
      runner_id TEXT NOT NULL,
      created_at TEXT NOT NULL
    );
  `);
}
```

- [ ] **Step 6: Implement repository**

Create `packages/store/src/repository.ts`:

```ts
import type { OpenTagEvent, OpenTagRun, OpenTagRunResult } from "@opentag/core";
import { eq } from "drizzle-orm";
import type { BetterSQLite3Database } from "drizzle-orm/better-sqlite3";
import { repoBindings, runEvents, runners, runs } from "./schema";

function nowIso(): string {
  return new Date().toISOString();
}

export function createOpenTagRepository(db: BetterSQLite3Database) {
  return {
    async registerRunner(input: { runnerId: string; name: string }): Promise<void> {
      const createdAt = nowIso();
      await db.insert(runners).values({ runnerId: input.runnerId, name: input.name, createdAt }).onConflictDoNothing();
    },

    async createRepoBinding(input: { provider: string; owner: string; repo: string; runnerId: string }): Promise<void> {
      await db.insert(repoBindings).values({ ...input, createdAt: nowIso() });
    },

    async createRun(input: { id: string; event: OpenTagEvent }): Promise<OpenTagRun> {
      const createdAt = nowIso();
      await db.insert(runs).values({
        id: input.id,
        eventId: input.event.id,
        status: "queued",
        eventJson: JSON.stringify(input.event),
        createdAt,
        updatedAt: createdAt
      });
      await db.insert(runEvents).values({
        runId: input.id,
        type: "run.created",
        payloadJson: JSON.stringify({ eventId: input.event.id }),
        createdAt
      });
      return {
        id: input.id,
        eventId: input.event.id,
        status: "queued",
        createdAt,
        updatedAt: createdAt
      };
    },

    async claimNextRun(input: { runnerId: string; leaseSeconds: number }): Promise<OpenTagRun | null> {
      const row = await db.select().from(runs).where(eq(runs.status, "queued")).limit(1).get();
      if (!row) return null;

      const updatedAt = nowIso();
      const leaseExpiresAt = new Date(Date.now() + input.leaseSeconds * 1000).toISOString();
      await db
        .update(runs)
        .set({
          status: "assigned",
          assignedRunnerId: input.runnerId,
          leaseExpiresAt,
          updatedAt
        })
        .where(eq(runs.id, row.id));
      await db.insert(runEvents).values({
        runId: row.id,
        type: "run.claimed",
        payloadJson: JSON.stringify({ runnerId: input.runnerId, leaseExpiresAt }),
        createdAt: updatedAt
      });

      return {
        id: row.id,
        eventId: row.eventId,
        status: "assigned",
        assignedRunnerId: input.runnerId,
        executor: row.executor ?? undefined,
        createdAt: row.createdAt,
        updatedAt
      };
    },

    async markRunning(input: { runId: string; executor: string }): Promise<void> {
      const updatedAt = nowIso();
      await db.update(runs).set({ status: "running", executor: input.executor, updatedAt }).where(eq(runs.id, input.runId));
      await db.insert(runEvents).values({
        runId: input.runId,
        type: "run.running",
        payloadJson: JSON.stringify({ executor: input.executor }),
        createdAt: updatedAt
      });
    },

    async completeRun(input: { runId: string; result: OpenTagRunResult }): Promise<void> {
      const updatedAt = nowIso();
      const status = input.result.conclusion === "success" ? "succeeded" : "failed";
      await db
        .update(runs)
        .set({ status, resultJson: JSON.stringify(input.result), updatedAt })
        .where(eq(runs.id, input.runId));
      await db.insert(runEvents).values({
        runId: input.runId,
        type: "run.completed",
        payloadJson: JSON.stringify(input.result),
        createdAt: updatedAt
      });
    }
  };
}
```

- [ ] **Step 7: Export store modules**

Create `packages/store/src/index.ts`:

```ts
export * from "./repository";
export * from "./schema";
```

- [ ] **Step 8: Install deps and run store test**

Run:

```bash
pnpm install
pnpm test packages/store/test/repository.test.ts
```

Expected: PASS.

## Task 4: Runner Interface And Echo Executor

**Files:**
- Create: `packages/runner/package.json`
- Create: `packages/runner/tsconfig.json`
- Create: `packages/runner/tsup.config.ts`
- Create: `packages/runner/src/executor.ts`
- Create: `packages/runner/src/echo.ts`
- Create: `packages/runner/src/index.ts`
- Create: `packages/runner/test/echo.test.ts`

- [ ] **Step 1: Write failing echo executor test**

Create `packages/runner/test/echo.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { createEchoExecutor } from "../src/echo";

describe("echo executor", () => {
  it("returns the command text as a successful result", async () => {
    const events: string[] = [];
    const executor = createEchoExecutor();
    const result = await executor.run(
      {
        runId: "run_1",
        workspacePath: "/tmp/demo",
        command: { rawText: "fix this", intent: "fix", args: {} },
        context: []
      },
      {
        emit: async (event) => {
          events.push(event.type);
        }
      }
    );

    expect(events).toContain("executor.started");
    expect(result.conclusion).toBe("success");
    expect(result.summary).toContain("fix this");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
pnpm test packages/runner/test/echo.test.ts
```

Expected: FAIL because runner modules do not exist.

- [ ] **Step 3: Create runner package metadata**

Create `packages/runner/package.json`:

```json
{
  "name": "@opentag/runner",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@opentag/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 4: Create runner TS configs**

Create `packages/runner/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

Create `packages/runner/tsup.config.ts`:

```ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
  sourcemap: true,
  clean: true
});
```

- [ ] **Step 5: Implement executor interface**

Create `packages/runner/src/executor.ts`:

```ts
import type { ContextPointer, OpenTagCommand, OpenTagRunResult } from "@opentag/core";

export type ExecutorEvent = {
  type: "executor.started" | "executor.progress" | "executor.completed" | "executor.failed";
  message: string;
  at: string;
};

export type ExecutorEventSink = {
  emit(event: ExecutorEvent): Promise<void>;
};

export type ExecutorRunInput = {
  runId: string;
  workspacePath: string;
  command: OpenTagCommand;
  context: ContextPointer[];
};

export type ExecutorReadiness = {
  ready: boolean;
  reason?: string;
};

export type ExecutorAdapter = {
  id: string;
  displayName: string;
  canRun(input: ExecutorRunInput): Promise<ExecutorReadiness>;
  run(input: ExecutorRunInput, sink: ExecutorEventSink): Promise<OpenTagRunResult>;
  cancel(runId: string): Promise<void>;
};
```

- [ ] **Step 6: Implement echo executor**

Create `packages/runner/src/echo.ts`:

```ts
import type { ExecutorAdapter } from "./executor";

function nowIso(): string {
  return new Date().toISOString();
}

export function createEchoExecutor(): ExecutorAdapter {
  return {
    id: "echo",
    displayName: "Echo Executor",
    async canRun() {
      return { ready: true };
    },
    async run(input, sink) {
      await sink.emit({
        type: "executor.started",
        message: `Echo executor started for ${input.runId}`,
        at: nowIso()
      });
      await sink.emit({
        type: "executor.completed",
        message: `Echo executor completed for ${input.runId}`,
        at: nowIso()
      });
      return {
        conclusion: "success",
        summary: `Echoed OpenTag command: ${input.command.rawText}`,
        verification: [
          {
            command: "echo",
            outcome: "passed",
            excerpt: input.command.rawText
          }
        ]
      };
    },
    async cancel() {
      return;
    }
  };
}
```

- [ ] **Step 7: Export runner modules**

Create `packages/runner/src/index.ts`:

```ts
export * from "./echo";
export * from "./executor";
```

- [ ] **Step 8: Run runner test**

Run:

```bash
pnpm test packages/runner/test/echo.test.ts
```

Expected: PASS.

## Task 5: GitHub Normalization And Comment Rendering

**Files:**
- Create: `packages/github/package.json`
- Create: `packages/github/tsconfig.json`
- Create: `packages/github/tsup.config.ts`
- Create: `packages/github/src/normalize.ts`
- Create: `packages/github/src/render.ts`
- Create: `packages/github/src/index.ts`
- Create: `packages/github/test/normalize.test.ts`

- [ ] **Step 1: Write failing normalization test**

Create `packages/github/test/normalize.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { normalizeGitHubIssueComment } from "../src/normalize";

describe("normalizeGitHubIssueComment", () => {
  it("normalizes an @opentag GitHub issue comment", () => {
    const event = normalizeGitHubIssueComment({
      id: "123",
      commentBody: "@opentag fix this",
      commentUrl: "https://github.com/acme/demo/issues/1#issuecomment-123",
      apiCommentsUrl: "https://api.github.com/repos/acme/demo/issues/1/comments",
      issueUrl: "https://github.com/acme/demo/issues/1",
      owner: "acme",
      repo: "demo",
      actorId: 42,
      actorLogin: "octocat",
      private: false,
      receivedAt: "2026-06-24T00:00:00.000Z"
    });

    expect(event?.source).toBe("github");
    expect(event?.command.intent).toBe("fix");
    expect(event?.metadata).toMatchObject({ owner: "acme", repo: "demo" });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
pnpm test packages/github/test/normalize.test.ts
```

Expected: FAIL because GitHub package does not exist.

- [ ] **Step 3: Create GitHub package metadata**

Create `packages/github/package.json`:

```json
{
  "name": "@opentag/github",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@opentag/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 4: Create GitHub TS configs**

Create `packages/github/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

Create `packages/github/tsup.config.ts`:

```ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
  sourcemap: true,
  clean: true
});
```

- [ ] **Step 5: Implement GitHub normalization**

Create `packages/github/src/normalize.ts`:

```ts
import { parseOpenTagMention, type OpenTagEvent } from "@opentag/core";

export type GitHubIssueCommentInput = {
  id: string;
  commentBody: string;
  commentUrl: string;
  apiCommentsUrl: string;
  issueUrl: string;
  owner: string;
  repo: string;
  actorId: number;
  actorLogin: string;
  private: boolean;
  receivedAt: string;
};

export function normalizeGitHubIssueComment(input: GitHubIssueCommentInput): OpenTagEvent | null {
  const mention = parseOpenTagMention(input.commentBody);
  if (!mention.matched) return null;

  return {
    id: `evt_github_comment_${input.id}`,
    source: "github",
    sourceEventId: input.id,
    receivedAt: input.receivedAt,
    actor: {
      provider: "github",
      providerUserId: String(input.actorId),
      handle: input.actorLogin
    },
    target: {
      mention: "@opentag",
      agentId: "opentag"
    },
    command: {
      rawText: mention.rawText,
      intent: mention.intent,
      args: mention.args
    },
    context: [
      {
        kind: "github.issue",
        uri: input.issueUrl,
        visibility: input.private ? "private" : "public"
      },
      {
        kind: "github.comment",
        uri: input.commentUrl,
        visibility: input.private ? "private" : "public"
      }
    ],
    permissions: [
      {
        scope: "issue:comment",
        reason: "reply to the source GitHub thread"
      },
      {
        scope: "runner:local",
        reason: "execute the run on a paired local daemon"
      }
    ],
    callback: {
      provider: "github",
      uri: input.apiCommentsUrl,
      threadKey: `${input.owner}/${input.repo}`
    },
    metadata: {
      owner: input.owner,
      repo: input.repo
    }
  };
}
```

- [ ] **Step 6: Implement comment rendering**

Create `packages/github/src/render.ts`:

```ts
import type { OpenTagRunResult } from "@opentag/core";

export function renderAcknowledgement(runId: string): string {
  return `OpenTag picked this up. Run: \`${runId}\``;
}

export function renderFinalResult(result: OpenTagRunResult): string {
  const lines = [`OpenTag finished with **${result.conclusion}**.`, "", result.summary];
  if (result.verification?.length) {
    lines.push("", "Verification:");
    for (const check of result.verification) {
      lines.push(`- \`${check.command}\`: ${check.outcome}`);
    }
  }
  if (result.nextAction) {
    lines.push("", `Next action: ${result.nextAction}`);
  }
  return lines.join("\n");
}
```

- [ ] **Step 7: Export GitHub modules**

Create `packages/github/src/index.ts`:

```ts
export * from "./normalize";
export * from "./render";
```

- [ ] **Step 8: Run GitHub tests**

Run:

```bash
pnpm test packages/github/test/normalize.test.ts
```

Expected: PASS.

## Task 6: Dispatcher HTTP API

**Files:**
- Create: `apps/dispatcher/package.json`
- Create: `apps/dispatcher/tsconfig.json`
- Create: `apps/dispatcher/tsup.config.ts`
- Create: `apps/dispatcher/src/server.ts`
- Create: `apps/dispatcher/src/index.ts`
- Create: `apps/dispatcher/test/server.test.ts`

- [ ] **Step 1: Write failing dispatcher API test**

Create `apps/dispatcher/test/server.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { createDispatcherApp } from "../src/server";

describe("dispatcher API", () => {
  it("creates and claims an echo run", async () => {
    const app = createDispatcherApp({ databasePath: ":memory:" });

    await app.request("/v1/runners", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ runnerId: "runner_1", name: "Local Runner" })
    });

    const createResponse = await app.request("/v1/runs", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        runId: "run_1",
        event: {
          id: "evt_1",
          source: "github",
          sourceEventId: "comment_1",
          receivedAt: "2026-06-24T00:00:00.000Z",
          actor: { provider: "github", providerUserId: "42", handle: "octocat" },
          target: { mention: "@opentag", agentId: "opentag" },
          command: { rawText: "fix this", intent: "fix", args: {} },
          context: [{ kind: "github.issue", uri: "https://github.com/acme/demo/issues/1", visibility: "public" }],
          permissions: [{ scope: "issue:comment", reason: "reply to source thread" }],
          callback: { provider: "github", uri: "https://api.github.com/repos/acme/demo/issues/1/comments" },
          metadata: { owner: "acme", repo: "demo" }
        }
      })
    });

    expect(createResponse.status).toBe(201);

    const claimResponse = await app.request("/v1/runners/runner_1/claim", { method: "POST" });
    expect(claimResponse.status).toBe(200);
    const claimed = await claimResponse.json();
    expect(claimed.run.id).toBe("run_1");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
pnpm test apps/dispatcher/test/server.test.ts
```

Expected: FAIL because dispatcher app does not exist.

- [ ] **Step 3: Create dispatcher package metadata**

Create `apps/dispatcher/package.json`:

```json
{
  "name": "@opentag/dispatcher",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@opentag/core": "workspace:*",
    "@opentag/store": "workspace:*",
    "better-sqlite3": "^11.7.0",
    "drizzle-orm": "^0.38.3",
    "hono": "^4.6.15"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 4: Create dispatcher TS configs**

Create `apps/dispatcher/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

Create `apps/dispatcher/tsup.config.ts`:

```ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  sourcemap: true,
  clean: true
});
```

- [ ] **Step 5: Implement dispatcher server**

Create `apps/dispatcher/src/server.ts`:

```ts
import { OpenTagEventSchema, OpenTagRunResultSchema } from "@opentag/core";
import { createOpenTagRepository, migrateSchema } from "@opentag/store";
import Database from "better-sqlite3";
import { drizzle } from "drizzle-orm/better-sqlite3";
import { Hono } from "hono";
import { z } from "zod";

const CreateRunnerSchema = z.object({
  runnerId: z.string().min(1),
  name: z.string().min(1)
});

const CreateRunSchema = z.object({
  runId: z.string().min(1),
  event: OpenTagEventSchema
});

const CompleteRunSchema = z.object({
  result: OpenTagRunResultSchema
});

export function createDispatcherApp(input: { databasePath: string }) {
  const sqlite = new Database(input.databasePath);
  migrateSchema(sqlite);
  const repo = createOpenTagRepository(drizzle(sqlite));
  const app = new Hono();

  app.post("/v1/runners", async (c) => {
    const parsed = CreateRunnerSchema.parse(await c.req.json());
    await repo.registerRunner(parsed);
    return c.json({ ok: true }, 201);
  });

  app.post("/v1/runs", async (c) => {
    const parsed = CreateRunSchema.parse(await c.req.json());
    const run = await repo.createRun({ id: parsed.runId, event: parsed.event });
    return c.json({ run }, 201);
  });

  app.post("/v1/runners/:runnerId/claim", async (c) => {
    const run = await repo.claimNextRun({ runnerId: c.req.param("runnerId"), leaseSeconds: 60 });
    if (!run) return c.json({ run: null }, 204);
    return c.json({ run }, 200);
  });

  app.post("/v1/runs/:runId/running", async (c) => {
    const body = z.object({ executor: z.string().min(1) }).parse(await c.req.json());
    await repo.markRunning({ runId: c.req.param("runId"), executor: body.executor });
    return c.json({ ok: true });
  });

  app.post("/v1/runs/:runId/complete", async (c) => {
    const parsed = CompleteRunSchema.parse(await c.req.json());
    await repo.completeRun({ runId: c.req.param("runId"), result: parsed.result });
    return c.json({ ok: true });
  });

  return app;
}
```

- [ ] **Step 6: Implement dispatcher entry point**

Create `apps/dispatcher/src/index.ts`:

```ts
import { serve } from "@hono/node-server";
import { createDispatcherApp } from "./server";

const port = Number(process.env.PORT ?? "3030");
const databasePath = process.env.OPENTAG_DATABASE_PATH ?? "opentag.db";

serve({
  fetch: createDispatcherApp({ databasePath }).fetch,
  port
});

console.log(`OpenTag dispatcher listening on http://localhost:${port}`);
```

- [ ] **Step 7: Install deps and run dispatcher test**

Run:

```bash
pnpm install
pnpm test apps/dispatcher/test/server.test.ts
```

Expected: PASS.

## Task 7: Local Daemon With Echo Executor

**Files:**
- Create: `apps/opentagd/package.json`
- Create: `apps/opentagd/tsconfig.json`
- Create: `apps/opentagd/tsup.config.ts`
- Create: `apps/opentagd/src/config.ts`
- Create: `apps/opentagd/src/client.ts`
- Create: `apps/opentagd/src/daemon.ts`
- Create: `apps/opentagd/src/index.ts`
- Create: `apps/opentagd/test/daemon.test.ts`

- [ ] **Step 1: Write failing daemon test**

Create `apps/opentagd/test/daemon.test.ts`:

```ts
import type { OpenTagRun } from "@opentag/core";
import { createEchoExecutor } from "@opentag/runner";
import { describe, expect, it } from "vitest";
import { runOneDaemonIteration } from "../src/daemon";

describe("opentagd", () => {
  it("claims a run and completes it with echo executor", async () => {
    const calls: string[] = [];
    const run: OpenTagRun = {
      id: "run_1",
      eventId: "evt_1",
      status: "assigned",
      assignedRunnerId: "runner_1",
      createdAt: "2026-06-24T00:00:00.000Z",
      updatedAt: "2026-06-24T00:00:00.000Z"
    };

    await runOneDaemonIteration({
      runnerId: "runner_1",
      workspacePath: "/tmp/demo",
      executor: createEchoExecutor(),
      client: {
        async claim() {
          calls.push("claim");
          return {
            run,
            command: { rawText: "fix this", intent: "fix", args: {} },
            context: []
          };
        },
        async markRunning(runId, executor) {
          calls.push(`running:${runId}:${executor}`);
        },
        async complete(runId, result) {
          calls.push(`complete:${runId}:${result.conclusion}`);
        }
      }
    });

    expect(calls).toEqual(["claim", "running:run_1:echo", "complete:run_1:success"]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
pnpm test apps/opentagd/test/daemon.test.ts
```

Expected: FAIL because daemon modules do not exist.

- [ ] **Step 3: Create opentagd package metadata**

Create `apps/opentagd/package.json`:

```json
{
  "name": "@opentag/opentagd",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "bin": {
    "opentagd": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@opentag/core": "workspace:*",
    "@opentag/runner": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 4: Create opentagd TS configs**

Create `apps/opentagd/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

Create `apps/opentagd/tsup.config.ts`:

```ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  sourcemap: true,
  clean: true
});
```

- [ ] **Step 5: Implement daemon client types**

Create `apps/opentagd/src/daemon.ts`:

```ts
import type { ContextPointer, OpenTagCommand, OpenTagRun, OpenTagRunResult } from "@opentag/core";
import type { ExecutorAdapter } from "@opentag/runner";

export type ClaimedRun = {
  run: OpenTagRun;
  command: OpenTagCommand;
  context: ContextPointer[];
};

export type DaemonClient = {
  claim(): Promise<ClaimedRun | null>;
  markRunning(runId: string, executor: string): Promise<void>;
  complete(runId: string, result: OpenTagRunResult): Promise<void>;
};

export async function runOneDaemonIteration(input: {
  runnerId: string;
  workspacePath: string;
  executor: ExecutorAdapter;
  client: DaemonClient;
}): Promise<boolean> {
  const claimed = await input.client.claim();
  if (!claimed) return false;

  await input.client.markRunning(claimed.run.id, input.executor.id);
  const result = await input.executor.run(
    {
      runId: claimed.run.id,
      workspacePath: input.workspacePath,
      command: claimed.command,
      context: claimed.context
    },
    {
      emit: async (event) => {
        console.log(`[${event.type}] ${event.message}`);
      }
    }
  );
  await input.client.complete(claimed.run.id, result);
  return true;
}
```

- [ ] **Step 6: Implement config loader**

Create `apps/opentagd/src/config.ts`:

```ts
export type OpenTagDaemonConfig = {
  runnerId: string;
  dispatcherUrl: string;
  workspacePath: string;
};

export function loadConfigFromEnv(): OpenTagDaemonConfig {
  return {
    runnerId: process.env.OPENTAG_RUNNER_ID ?? "runner_local",
    dispatcherUrl: process.env.OPENTAG_DISPATCHER_URL ?? "http://localhost:3030",
    workspacePath: process.env.OPENTAG_WORKSPACE_PATH ?? process.cwd()
  };
}
```

- [ ] **Step 7: Implement dispatcher client**

Create `apps/opentagd/src/client.ts`:

```ts
import type { DaemonClient } from "./daemon";

export function createDispatcherClient(input: { dispatcherUrl: string; runnerId: string }): DaemonClient {
  const baseUrl = input.dispatcherUrl.replace(/\/$/, "");

  return {
    async claim() {
      const response = await fetch(`${baseUrl}/v1/runners/${input.runnerId}/claim`, { method: "POST" });
      if (response.status === 204) return null;
      if (!response.ok) throw new Error(`claim failed: ${response.status}`);
      const body = (await response.json()) as { run: { id: string; eventId: string; status: "assigned"; createdAt: string; updatedAt: string }; event?: unknown };
      if (!body.run) return null;
      return {
        run: body.run,
        command: { rawText: "echo run", intent: "run", args: {} },
        context: []
      };
    },
    async markRunning(runId, executor) {
      await fetch(`${baseUrl}/v1/runs/${runId}/running`, {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ executor })
      });
    },
    async complete(runId, result) {
      await fetch(`${baseUrl}/v1/runs/${runId}/complete`, {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ result })
      });
    }
  };
}
```

- [ ] **Step 8: Implement CLI entry point**

Create `apps/opentagd/src/index.ts`:

```ts
#!/usr/bin/env node
import { createEchoExecutor } from "@opentag/runner";
import { Command } from "commander";
import { createDispatcherClient } from "./client";
import { loadConfigFromEnv } from "./config";
import { runOneDaemonIteration } from "./daemon";

const program = new Command();

program
  .name("opentagd")
  .description("OpenTag local daemon")
  .command("run-once")
  .description("Claim and execute one run if available")
  .action(async () => {
    const config = loadConfigFromEnv();
    const didWork = await runOneDaemonIteration({
      runnerId: config.runnerId,
      workspacePath: config.workspacePath,
      executor: createEchoExecutor(),
      client: createDispatcherClient({
        dispatcherUrl: config.dispatcherUrl,
        runnerId: config.runnerId
      })
    });
    console.log(didWork ? "OpenTag run completed" : "No OpenTag run available");
  });

await program.parseAsync(process.argv);
```

- [ ] **Step 9: Run daemon test**

Run:

```bash
pnpm test apps/opentagd/test/daemon.test.ts
```

Expected: PASS.

## Task 8: Probot GitHub App Ingress

**Files:**
- Create: `apps/github-probot/package.json`
- Create: `apps/github-probot/tsconfig.json`
- Create: `apps/github-probot/tsup.config.ts`
- Create: `apps/github-probot/src/app.ts`
- Create: `apps/github-probot/src/index.ts`
- Create: `apps/github-probot/test/app.test.ts`

- [ ] **Step 1: Write failing Probot app unit test**

Create `apps/github-probot/test/app.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { handleIssueCommentCreated } from "../src/app";

describe("GitHub Probot handler", () => {
  it("creates a dispatcher run for an opentag mention", async () => {
    const createRun = vi.fn(async () => ({ runId: "run_1" }));
    const postComment = vi.fn(async () => undefined);

    await handleIssueCommentCreated({
      payload: {
        comment: {
          id: 123,
          body: "@opentag fix this",
          html_url: "https://github.com/acme/demo/issues/1#issuecomment-123"
        },
        issue: {
          html_url: "https://github.com/acme/demo/issues/1",
          comments_url: "https://api.github.com/repos/acme/demo/issues/1/comments"
        },
        repository: {
          name: "demo",
          private: false,
          owner: { login: "acme" }
        },
        sender: {
          id: 42,
          login: "octocat"
        }
      },
      createRun,
      postComment,
      now: () => "2026-06-24T00:00:00.000Z"
    });

    expect(createRun).toHaveBeenCalledOnce();
    expect(postComment).toHaveBeenCalledWith("OpenTag picked this up. Run: `run_1`");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
pnpm test apps/github-probot/test/app.test.ts
```

Expected: FAIL because app handler does not exist.

- [ ] **Step 3: Create GitHub Probot package metadata**

Create `apps/github-probot/package.json`:

```json
{
  "name": "@opentag/github-probot",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "probot run ./src/index.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@opentag/github": "workspace:*",
    "probot": "^13.4.8"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 4: Create Probot TS configs**

Create `apps/github-probot/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

Create `apps/github-probot/tsup.config.ts`:

```ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  sourcemap: true,
  clean: true
});
```

- [ ] **Step 5: Implement testable issue comment handler**

Create `apps/github-probot/src/app.ts`:

```ts
import { normalizeGitHubIssueComment, renderAcknowledgement } from "@opentag/github";
import type { Probot } from "probot";

type IssueCommentPayload = {
  comment: { id: number; body: string; html_url: string };
  issue: { html_url: string; comments_url: string };
  repository: { name: string; private: boolean; owner: { login: string } };
  sender: { id: number; login: string };
};

export async function handleIssueCommentCreated(input: {
  payload: IssueCommentPayload;
  createRun(event: unknown): Promise<{ runId: string }>;
  postComment(body: string): Promise<void>;
  now(): string;
}): Promise<void> {
  const event = normalizeGitHubIssueComment({
    id: String(input.payload.comment.id),
    commentBody: input.payload.comment.body,
    commentUrl: input.payload.comment.html_url,
    apiCommentsUrl: input.payload.issue.comments_url,
    issueUrl: input.payload.issue.html_url,
    owner: input.payload.repository.owner.login,
    repo: input.payload.repository.name,
    actorId: input.payload.sender.id,
    actorLogin: input.payload.sender.login,
    private: input.payload.repository.private,
    receivedAt: input.now()
  });

  if (!event) return;

  const { runId } = await input.createRun(event);
  await input.postComment(renderAcknowledgement(runId));
}

export function createOpenTagProbotApp(app: Probot): void {
  app.on("issue_comment.created", async (context) => {
    await handleIssueCommentCreated({
      payload: context.payload as IssueCommentPayload,
      createRun: async () => {
        const runId = `run_${Date.now()}`;
        return { runId };
      },
      postComment: async (body) => {
        await context.octokit.issues.createComment(context.issue({ body }));
      },
      now: () => new Date().toISOString()
    });
  });
}
```

- [ ] **Step 6: Implement Probot entry point**

Create `apps/github-probot/src/index.ts`:

```ts
import { createOpenTagProbotApp } from "./app";

export default createOpenTagProbotApp;
```

- [ ] **Step 7: Run Probot app test**

Run:

```bash
pnpm test apps/github-probot/test/app.test.ts
```

Expected: PASS.

## Task 9: Demo Documentation

**Files:**
- Create: `README.md`
- Create: `examples/github-to-echo/README.md`
- Modify: `docs/design.md`

- [ ] **Step 1: Write root README**

Create `README.md`:

```md
# OpenTag

OpenTag is the open mention layer for agents.

Tag Codex, Claude Code, Pi, or your own local runner from GitHub, Slack, or Lark. OpenTag turns the mention into an auditable run, dispatches it to an approved local or hosted executor, and reports the result back to the original workspace.

## Why

Claude Tag brings Claude into Slack. OpenTag brings any agent into any workspace.

## V0 Direction

The first implementation focuses on a narrow GitHub-to-local-runner loop:

1. A GitHub comment mentions `@opentag`.
2. A Probot app normalizes the event.
3. A thin hosted dispatcher stores and leases the run.
4. A local daemon claims the run.
5. An executor adapter runs the task.
6. OpenTag reports the result back to GitHub.

## Packages

- `packages/core`: protocol schemas and mention parsing.
- `packages/github`: GitHub event normalization and callback rendering.
- `packages/store`: SQLite/Drizzle persistence and lease primitives.
- `packages/runner`: executor contracts and the echo executor.
- `apps/dispatcher`: hosted dispatcher API.
- `apps/opentagd`: local runner daemon.
- `apps/github-probot`: GitHub App ingress.

## Commands

```bash
pnpm install
pnpm test
pnpm build
```

## Design

See [`docs/design.md`](docs/design.md).
```

- [ ] **Step 2: Write demo README**

Create `examples/github-to-echo/README.md`:

```md
# GitHub To Echo Demo

This demo proves the OpenTag v0 loop without needing a real coding agent.

## Flow

```text
@opentag fix this
-> GitHub Probot ingress
-> hosted dispatcher run
-> opentagd local daemon
-> echo executor
-> final callback text
```

## Local Manual Path

1. Start the dispatcher:

```bash
OPENTAG_DATABASE_PATH=opentag.db pnpm --filter @opentag/dispatcher dev
```

2. Register a runner:

```bash
curl -X POST http://localhost:3030/v1/runners \
  -H 'content-type: application/json' \
  -d '{"runnerId":"runner_local","name":"Local Runner"}'
```

3. Create a run with a normalized event payload.

4. Run the daemon once:

```bash
OPENTAG_RUNNER_ID=runner_local \
OPENTAG_DISPATCHER_URL=http://localhost:3030 \
pnpm --filter @opentag/opentagd dev -- run-once
```
```

- [ ] **Step 3: Update design status**

Modify `docs/design.md` status line from:

```md
Draft, 2026-06-24
```

to:

```md
Implementation-ready draft, 2026-06-24
```

- [ ] **Step 4: Run final checks**

Run:

```bash
pnpm test
pnpm build
pnpm -r lint
```

Expected: all pass.

## Self-Review Checklist

- Spec coverage: the plan implements the GitHub mention, core event model, dispatcher, local daemon, echo executor, and callback rendering required by `docs/design.md`.
- Deferred by design: real Codex/Claude Code/oh-my-pi execution, PR creation, Slack/Lark apps, JSON Schema export, hosted auth hardening, and durable workflow platforms.
- Risk: Task 7's first dispatcher client does not yet return full original event data on claim. This is acceptable for echo but must be fixed before a real coding executor.
- Resolved: Task 8's Probot app now creates runs through the dispatcher HTTP API, and can defer acknowledgement comments when dispatcher-owned callbacks are enabled.
